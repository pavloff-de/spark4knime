Input:
    File to RDD
    Table to RDD

Transformations:
    map
    flatMap
    filter
    sample
    union
    intersection
    distinct
    groupByKey
    reduceByKey
    sortByKey
    join
    cogroup

Action:
    reduce
    collect
    count
    countByKey
    first
    take
    takeSample
    foreach

Output:
    saveAsTextFile
    saveAsSequenceFile
